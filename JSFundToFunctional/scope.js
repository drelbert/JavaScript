scope 

is = "area where a variable has access to some value"

ruleFunction = [
"has access to its own local scope variables",
"has access to the variables contained within the same scope that function was created in",
"the functions local scope variables are available anywhere outside that function"
]

ruleInputs = "inputs are treated as local scope variables"

ruleLet = "blocks scope can be treated with let"