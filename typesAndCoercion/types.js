primitiveTypes;

ECMAPrimitveTypes = [
  "Undefined",
  "Null",
  "Boolean",
  "String",
  "Symbol",
  "Number",
  "Object",
];

JSNote = "variables do not have types -> values do";

typeOfOperator;

returns = "looking at the value type?";

var v;
typeof v; // "undefined"

kindsOfEmptyness;

differentiate = "undefined and undeclared";

undefined = "means there is a variable, but at this moment it has no value";

undeclared = "its never been created in any scope";

andAlso = "unitialized aka TDZ, block scoped variables never get initialized";

NaN - IsNaN;

NaN = "means spcial centinal value refering to an invalid number";

fundaMentalObjects;

are = "sort of types, but bolted on object likeness";

ifNeedToContstruct = [
  "use new with",
  "object()",
  "array()",
  "function()",
  "date()",
];
