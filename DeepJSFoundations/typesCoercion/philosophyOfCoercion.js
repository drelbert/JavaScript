//Given corner cases, let's adopt a coding style to deal with them

const codingStyle = 'make value types plain and obvious';

const design = 'one function that only takes numbers, another that only takes strings';

let point = 'dynamic typing is a string quality, rather than a weakness';

let codeAsCommunication = 'write code so that it effectively communicates intent to the reader';

implicit = Abstracted;

const useful = 'when reader can focus on the important'

const dangerous = 'when the reader cannot tell what will happen'

const better = 'when the reader can understand the code'