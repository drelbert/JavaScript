// traverse one tree from child node to parent
// in order to determine location of same child node in a copy of the tree

var treeTwoProperties = ["root", "node"];
var nodes = ["parent", "child"];
var representParentChild = [0, 1]; // parent.children = array of items

function reversePath() {
  // statement to traverse the tree
  // check and record the path
}
